---
type Props = {
    title?: string;
    link?: string;
}

const { title, link } = Astro.props;
---
<a href={link}>
    <div class={`bg-gray-100 py-2 px-4 m-2 shadow-md ${link && "hover:shadow-lg cursor-pointer"}`}>
        <h3 class="text-xl my-2">{title}</h3>
        <slot />
    </div>
</a>