---
import BlogNavigation from "@components/BlogNavigation.astro";
import PageLayout from "./PageLayout.astro";

type Props = {
    pageTitle: string;
}

const { pageTitle } = Astro.props;
---
<PageLayout pageTitle={`${pageTitle} — mioi Blog`}>
    <BlogNavigation />
    <slot />
</PageLayout>